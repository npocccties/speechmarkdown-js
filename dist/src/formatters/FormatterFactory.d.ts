import { Formatter } from '../Interfaces';
import { SpeechOptions } from '../SpeechOptions';
export declare function createFormatter(options: SpeechOptions): Formatter;
export declare function createTextFormatter(options: SpeechOptions): Formatter;
